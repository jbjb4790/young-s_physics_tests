/**
 * 이 파일은 GitHub Actions 배포 과정에서 저장소 변수 YP_API_URL 값으로 자동 생성됩니다.
 * 로컬 미리보기에서는 빈 값이면 데모 모드로 동작합니다.
 * Apps Script /exec URL은 공개 주소이므로 이 파일에 들어가도 되지만 PIN·세션·WRITE_KEY는 절대 넣지 마세요.
 */
window.YP_RUNTIME_CONFIG = window.YP_RUNTIME_CONFIG || {
  apiUrl: "",
  buildVersion: "3.2.0-excel-batch-import"
};
